// Delay
Future buttonDelay() {
  return Future.delayed(const Duration(milliseconds: 185), () {});
}

// Bouncing Animation
const Duration animationLength = Duration(milliseconds: 75);

// Bouncing Size
const double bouncingSize = 1.5;