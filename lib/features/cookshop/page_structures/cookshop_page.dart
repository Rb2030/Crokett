import 'package:flutter/material.dart';

class CookShopPage extends StatelessWidget {

  const CookShopPage();

  @override
  Widget build(BuildContext context) {
    return Center(child: Container(color: Colors.green, child: Text('COOKSHOP PAGE')));
  }
}