import 'package:flutter/material.dart';

class BoxesPage extends StatelessWidget {

  const BoxesPage();

  @override
  Widget build(BuildContext context) {
    return Center(child: Container(color: Colors.red, child: Text('BOXES PAGE')));
  }
}